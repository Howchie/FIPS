% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_mccauley2024.R
\name{mccauley2024_make_inits}
\alias{mccauley2024_make_inits}
\title{Compute steady-state initial conditions for the McCauley (2024) model}
\usage{
mccauley2024_make_inits(
  sim_start,
  sleep_hrs = 8,
  wake_time = "07:30:00",
  ndays = 30,
  tz = NULL,
  round_minutes = 30,
  pvec = mccauley2024_pvec,
  substep_minutes = 2,
  return_equilibrium = FALSE,
  return_adaptation = FALSE,
  save_equilibrium_path = NULL
)
}
\arguments{
\item{sim_start}{POSIXct. Timestamp of the *real* simulation start; only the
time-of-day is used to pick initial states on the final equilibrium day.}

\item{sleep_hrs}{Numeric hours of nightly sleep (default 8).}

\item{wake_time}{Character "HH:MM:SS" (local clock) for daily wake time (default "07:30:00").}

\item{ndays}{Integer number of adaptation days (default 30).}

\item{tz}{Timezone (default taken from `sim_start`, else "UTC").}

\item{round_minutes}{Rounding used by `parse_sleeptimes` (default 30). Must divide 60.}

\item{pvec}{Parameter vector used for the adaptation run. `p0/u0/k0/h0` in
`pvec` are ignored; the returned inits will replace them. Defaults to `mccauley2024_pvec`.}

\item{substep_minutes}{Internal ODE substep (default 2) passed to the simulator.}

\item{return_equilibrium}{If TRUE, also return the final-day dataframe.}

\item{save_equilibrium_path}{Optional path to save the final-day equilibrium (RDS).}
}
\value{
Named vector c(p0=..., u0=..., k0=..., h0=...) or a list with
  $inits and $equilibrium if `return_equilibrium=TRUE`.
}
\description{
Simulates a stable schedule for `ndays` to approximate equilibrium, then
returns p0/u0/k0/h0 at the requested simulation start time-of-day.
}
